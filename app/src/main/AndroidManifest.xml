<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.datingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DatingApp">
        <activity android:name=".SwapActivity"></activity>
        <activity android:name=".LoginAndPasswordActivity" />
        <activity android:name=".GenderActivity" />
        <activity android:name=".SetDateActivity" />
        <activity android:name=".PasswordActivity" />
        <activity android:name=".EmailActivity" />
        <activity android:name=".NameActivity" />
        <activity android:name=".LoginAndSingInActivity" />
        <activity android:name=".ForgotPassowrdActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>